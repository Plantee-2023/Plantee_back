<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plantee.mapper.CommMapper">
	<select id="list" resultType="hashmap">
		SELECT u.address1, u.uid, u.nickname,
    (SELECT SUBSTRING_INDEX(u.address1, ' ', 1) AS address
     FROM users u 
     WHERE p.uid = u.uid
     LIMIT 1
    ) AS address, p.*
    FROM posts p, users u 
    WHERE p.uid = u.uid and p.category=3
    and
    where title like  concat('%',#{query},'%') or contents like concat('%',#{query},'%')
   
    ORDER BY p.post_id DESC
    LIMIT #{start}, #{size};
	</select>
	
	
	<select id="list2" resultType="hashmap">
		SELECT u.address1, u.uid, u.nickname,
    (SELECT SUBSTRING_INDEX(u.address1, ' ', 1) AS address
     FROM users u 
     WHERE p.uid = u.uid
     LIMIT 1
    ) AS address, p.*
    FROM posts p, users u 
    WHERE p.uid = u.uid and p.category=4
    
   
 
    ORDER BY p.post_id DESC
    LIMIT #{start}, #{size};
	</select>
	
	
	 <insert id="insert">
    	insert into posts (category, uid,   title,contents,price,filter)
    	values(#{category}, #{uid}, #{title}, #{contents},#{price},#{filter});
    </insert>
    
    <insert id="insert_reply">
    	insert into posts (category, uid,   title,contents,price,filter,post_origin,reply)
    	values(#{category}, #{uid}, #{title}, #{contents},#{price},#{filter},#{post_origin},#{reply});
    </insert>
    
    <select id="reply_total" resultType="int">
		 select count(*) 
    from posts
    where post_origin=#{post_origin};
		</select>
    
    <delete id="delete">
		delete from posts
		where post_id=#{post_id}
	</delete>
	
	
	<update id="update">
		update posts
		set contents=#{contents},  
		where post_id=#{post_id}
	</update>
	
	<select id="read" resultType="hashmap">
		 SELECT u.address1, u.uid, u.nickname,
    (SELECT SUBSTRING_INDEX(u.address1, ' ', 1) AS address
     FROM users u 
     WHERE p.uid = u.uid
     LIMIT 1
    ) AS address, p.*
    FROM posts p, users u 
	where p.uid = u.uid and p.post_id=#{post_id}
		</select>
	
	
	 <select id="total" resultType="int">
    	select count(*)
    	from posts;
    </select>
    
       <select id="address1" resultType="String">
		(SELECT SUBSTRING(address1, 1, INSTR(address1, ' ') - 1) AS address
FROM users)
WHERE uid = #{uid};
    </select>
    
    
 
 
 
</mapper>

	
    
    
 


