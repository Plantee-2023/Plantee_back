<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.CommMapper">
	<select id="list" resultType="hashmap">
		select * 
		from posts
		
		order by post_id desc
		limit #{start}, #{size};
	</select>
	
	
	 <insert id="insert">
    	insert into posts (category, uid, title,contents)
    	values(#{category}, #{uid},#{title}, #{contents});
    </insert>
    
    <delete id="delete">
		delete from posts
		where post_id=#{post_id}
	</delete>
	
	
	<update id="update">
		update posts
		set title=#{title},  
		where post_id=#{post_id}
	</update>
	
	
	 <select id="total" resultType="int">
    	select count(*)
    	from posts;
    </select>
    
    
</mapper>


